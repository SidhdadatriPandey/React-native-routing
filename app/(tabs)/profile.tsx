import React, { Component } from 'react'
import { Text, View } from 'react-native'

export default class profile extends Component {
  render() {
    return (
      <View>
        <Text style={{color:'black', fontSize:30, paddingTop:150}}> textInComponent </Text>
      </View>
    )
  }
}
